"""Low-level signal / grapho-element primitives used during model fitting.

These are vendored copies of the event-detection routines so that the simulator
can measure spindle / K-complex rates in its own simulated signals without
depending on the separate ``kcomplex-detector`` project.
"""
